import 'package:flutter/material.dart';

class DetailPage extends StatelessWidget {
  static Route route() => MaterialPageRoute(builder: (_) => DetailPage());

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
